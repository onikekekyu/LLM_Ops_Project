# Chemin racine pour Python
PYTHONPATH=.

# ----------------------------
# Configuration GCP (remplacez par vos valeurs)
# ----------------------------
# ID du projet GCP (ex: my-gcp-project)
GCP_PROJECT_ID=your-project-id
# Région GCP (ex: europe-west2)
GCP_REGION=europe-west2
# Nom du bucket GCS pour les données brutes
GCP_BUCKET_NAME=your-raw-data-bucket
# URI optionnel du dataset (surcharge RAW_DATASET si nécessaire)
RAW_DATASET_URI=gs://your-raw-data-bucket/your_dataset_filename.csv
# Numéro du projet (optionnel, certaines API l'exigent)
GCP_PROJECT_NUMBER=your-project-number
# ID d'un endpoint AI Platform (optionnel)
GCP_ENDPOINT_ID=your-endpoint-id

# ----------------------------
# Langfuse / Observabilité (laisser vide si non utilisé)
# ----------------------------
LANGFUSE_SECRET_KEY=your-langfuse-secret-key
LANGFUSE_PUBLIC_KEY=your-langfuse-public-key
LANGFUSE_HOST=http://localhost:3000/

# ----------------------------
# Chemins et fichiers
# ----------------------------
# Chemin vers un fichier JSON d'exemple pour les requêtes d'inférence
INPUT_DATA_FILE="./data/sample_request.json"

# ----------------------------
# Secrets locaux / chiffrement (NE PAS COMMITTER DE VALEURS RÉELLES)
# ----------------------------
# secret pour next-auth ou équivalent
NEXTAUTH_SECRET=replace-with-a-secure-random-value
# sel ou salt pour hachage
SALT=replace-with-a-secure-random-value
# clé de chiffrement (hex/base64) utilisée localement
ENCRYPTION_KEY=replace-with-a-secure-random-value

# ----------------------------
# Bases de données et URLs (pour stack local ou Langfuse)
# ----------------------------
POSTGRES_USER=postgres
POSTGRES_PASSWORD=localpassword
POSTGRES_DB=langfuse
# Exemple d'URL SQLAlchemy
DATABASE_URL=postgresql://postgres:localpassword@postgres:5432/langfuse

# ClickHouse
CLICKHOUSE_USER=clickhouse
CLICKHOUSE_PASSWORD=localpassword

# MinIO / S3 (pour stockage d'artefacts locaux)
MINIO_ROOT_USER=minio
MINIO_ROOT_PASSWORD=localpassword

# Redis (auth optional)
REDIS_AUTH=localpassword

# ----------------------------
# Clés S3 pour les modules qui uploadent vers S3/MinIO (ex: Langfuse)
# Utiliser des clés correspondant aux identifiants MINIO_ROOT_*
# ----------------------------
LANGFUSE_S3_EVENT_UPLOAD_ACCESS_KEY_ID=minio
LANGFUSE_S3_EVENT_UPLOAD_SECRET_ACCESS_KEY=localpassword
LANGFUSE_S3_MEDIA_UPLOAD_ACCESS_KEY_ID=minio
LANGFUSE_S3_MEDIA_UPLOAD_SECRET_ACCESS_KEY=localpassword
LANGFUSE_S3_BATCH_EXPORT_ACCESS_KEY_ID=minio
LANGFUSE_S3_BATCH_EXPORT_SECRET_ACCESS_KEY=localpassword

# URL externe pour NextAuth / Callback
NEXTAUTH_URL=http://localhost:3000

